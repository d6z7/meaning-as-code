# TRANSIT (example) — Physical layer: the line register descriptor.
metadata:
  table: line_register
  source: TRANSIT
  version: '1.0'
  schema_version: '0.1.9'
  status: verified
  owner: example-team
  confidence: C

table:
  name: line_register
  schema: transit
  type: table
  description: >
    One row per transit line. THE register of which line codes exist — grounding target for the Line
    concept, and the thing a line code must be looked up in rather than assembled.

columns:
  - { name: line_code,  type: string, role: primary_key }
  - { name: line_label, type: string, role: value }
  - { name: mode,       type: string, role: discriminator }

grounded_by_concepts:
  - { concept: Line, role: primary_table }

governance:
  owner: example-team
  last_reviewed: 2026-09-12
  change_log:
    - { date: 2026-09-12, change: "Created — transit reference bundle.", change_type: CREATION, by: example-team }
