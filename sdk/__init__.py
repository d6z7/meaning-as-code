"""sdk — the managed authoring framework (build-time, VSC only).

The only code permitted to write the ontology SSOT (`sources/**`), via
`sdk.authoring.operations`. See ../boundaries.yaml and
../decisions/2026-08-12_three-plane-authoring-model.md.
"""
