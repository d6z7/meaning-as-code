# NEGATIVE fixture — MUST be rejected: 'foreign_key' is not a legal BUSINESS edge type.
# v0.1.12: a business edge type must be `identity` or `shared_attribute` (foreign_key is physical-only).
edges:
  - edge_id: a__related__b
    level: business
    type: foreign_key     # ← illegal: business ⊂ {identity, shared_attribute}
    endpoints:
      # Fully specified on purpose (v0.1.18 requires a cardinality on both ends): the ONLY
      # defect in this fixture must be the illegal `type`, or it stops testing the type rule.
      from: { concept: A, ref: a.yaml#concept, cardinality: "0..N" }
      to:   { concept: B, ref: b.yaml#concept, cardinality: "1" }
    resolved_by: "rules.yaml#rules.x"
