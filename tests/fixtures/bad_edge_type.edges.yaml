# NEGATIVE fixture — MUST be rejected: 'denormalized' is not a legal physical edge type.
edges:
  - edge_id: a__denorm__b
    level: physical
    type: denormalized    # ← illegal: physical type must be foreign_key/composite_key/value_mapped_key
    endpoints:
      # Fully specified on purpose: the ONLY defect must be the illegal `type`.
      from: { concept: A, ref: a.yaml#concept, cardinality: "0..N" }
      to:   { concept: B, ref: b.yaml#concept, cardinality: "1" }
