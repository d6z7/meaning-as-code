# SHOP (example) — Physical layer: the products table descriptor
# Synthetic example — illustrates the physical (grounding-target) layer. Not real data.

metadata:
  table: products
  source: SHOP
  version: '1.0'
  schema_version: '0.1.9'
  status: verified                 # in a real setting: reconciled against the live DB via DESCRIBE
  owner: example-team
  confidence: C

table:
  name: products
  schema: shop_warehouse
  type: table
  description: "One row per product (sku). Grounding target for the Product reference dimension."

columns:
  - { name: sku,         type: string, role: primary_key }
  - { name: name,        type: string, role: value, x-subrole: attribute }
  - { name: category_id, type: string, role: foreign_key }
  - { name: list_price,  type: decimal, role: value, x-subrole: attribute }

foreign_keys:
  - { name: fk_category, from_column: category_id, to_table: categories, to_column: category_id }

grounded_by_concepts:
  - { concept: Product, role: primary_table }

governance:
  owner: example-team
  last_reviewed: 2026-06-08
  change_log:
    - { date: 2026-06-08, change: "Created to complete the example's physical layer (referenced by the belongs_to_category edge).", change_type: CREATION, by: example-team }
