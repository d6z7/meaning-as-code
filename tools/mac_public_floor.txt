# check_mac_public RATCHET FLOOR — the measured count this tree is allowed to carry.
#
# 300 -> 146 -> 0.
#
# 300 was the count before the SDK was consolidated into this repo. 146 was measured after the first
# cleanse pass and, critically, NOBODY LOWERED IT: the count fell to 4 while the floor still declared
# 146, so the gate would have printed PASS at 145 findings. A floor above the measurement is not a
# ratchet — it is 141 findings of silent headroom. The gate now reports its own slack for exactly
# that reason.
#
# 0 is measured, 2026-09-13: `PASS: check_mac_public — 0 leak(s) over 487 tracked file(s) examined`.
#
# ZERO IS NOW THE CONTRACT. Any new finding fails the gate, which is the point: this is a PUBLIC
# repository and the instrument that keeps an estate's identity out of it should have no tolerance
# left to spend. If a genuine, reviewed use needs a token, the per-line escape hatch exists and is
# visible in the diff — raising this number is not the answer.
0
